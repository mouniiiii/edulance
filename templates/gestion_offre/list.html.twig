<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Offers</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            animation: slideIn 0.5s ease;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h2 {
            color: #153462;
            margin-bottom: 20px;
            text-align: center;
        }

        .offer-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .offer-table th, .offer-table td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        .offer-table th {
            background-color: #f2f2f2;
        }

        .offer-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .offer-table tr:hover {
            background-color: #f2f2f2;
        }

        .btn {
            display: inline-block;
            background-color: #007bff;
            color: #fff;
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s ease-in-out;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>List of Offers</h2>
        <table class="offer-table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Type</th>
                    <th>Experience Level</th>
                    <th>Salary</th>
                    <th>Expiration Date</th>
                    <th>Description</th>
                    <th>Language</th>
                </tr>
            </thead>
            <tbody>
                {% for offer in offres %}
                    <tr>
                        <td>{{ offer.Titre }}</td>
                        <td>{{ offer.Type_Offre }}</td>
                        <td>{{ offer.Experience_Level }}</td>
                        <td>{{ offer.Salary }}</td>
                        <td>{{ offer.Expiration_Date|date('Y-m-d') }}</td>
                        <td>{{ offer.Description }}</td>
                        <td>{{ offer.Language }}</td>
                        <!--<td>
                            <a href="{{ path('editoffer', {'id': offer.id}) }}" class="btn">Edit</a>
                            <a href="{{ path('deleteoffer', {'id': offer.id}) }}" class="btn">Delete</a>
                        </td>-->
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>

